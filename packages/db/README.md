# UAPS Database Package

This package stores SQL schema and migration files for the UAPS MVP.

## Quick Start

1. Create a PostgreSQL database.
2. Run [sql/001_init_uaps.sql](sql/001_init_uaps.sql).
3. Point the API service to your database with `DATABASE_URL`.
